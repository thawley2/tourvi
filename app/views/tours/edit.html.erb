<% content_for :title, "Edit #{@tour.name}" %>
<div class="mx-auto max-w-2xl px-4 py-8">
  <h1 class="mb-5 font-display text-2xl">Edit Tour</h1>
  <%= render "form", tour: @tour, submit_label: "Save Changes" %>
</div>
